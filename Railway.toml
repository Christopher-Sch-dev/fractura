[build]
builder = "dockerfile"
dockerfile = "backend/Dockerfile"

[deploy]
rootDirectory = "backend"

[environment]
PYTHONUNBUFFERED = "1"
LOCAL_DATA_PATH = "/app/data"
DB_MODE = "duckdb"
RAILWAY_DEPLOY_REGION = "us-east1"